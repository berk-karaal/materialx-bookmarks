# Tools

Command-line tools and editors worth keeping on a new machine.

This collection opens in **block** mode with columns no narrower than `14rem`, and offers 6, 12 or
all tools per page. Narrow the window and the grid drops a column at a time — it measures the
component, not the browser.

```bookmarks
collection: tools
```

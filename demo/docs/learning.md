# Learning

Documentation, specifications and books.

The last entry in this collection has no `url`. A bookmark only requires a `title`, so entries like
that render as plain text instead of a link.

```bookmarks
collection: learning
```

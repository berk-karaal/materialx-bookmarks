# Learning

Documentation, specifications and books.

The last entry in this collection has no `links`. A bookmark only requires a `title`, so entries
like that render as plain text with no buttons beneath them.

Several entries here carry more than one link — the Python docs point at both the reference and the
tutorial, and WCAG 2.2 at both the specification and its quick reference.

```bookmarks
collection: learning
```

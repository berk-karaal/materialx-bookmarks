# Reading

Blogs that reliably repay the time spent on them.

These descriptions are long enough that a grid would mean a lot of scrolling, so this collection
offers only **list** and **compact** — its switcher has two buttons rather than three. It has no
page-size picker either, because its configuration does not offer one.

```bookmarks
collection: reading
```

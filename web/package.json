{
  "name": "materialx-bookmarks-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "fuse.js": "^7.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vitest": "^2.0.0"
  }
}
